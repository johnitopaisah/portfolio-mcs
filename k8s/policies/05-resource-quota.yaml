# apiVersion: v1
# kind: ResourceQuota
# metadata:
#   name: portfolio-quota
#   namespace: portfolio
#   labels:
#     app: portfolio-mcs
# spec:
#   hard:
#     # Sized for Minikube default (2 vCPU / 4 GB RAM, ~3 GB usable by pods).
#     # When moving to a dedicated cloud node, increase these accordingly:
#     #   requests.cpu: "2"  →  limits.cpu: "4"
#     #   requests.memory: 2Gi  →  limits.memory: 6Gi
#     requests.cpu:    "1"
#     requests.memory: "1Gi"
#     limits.cpu:      "2"
#     limits.memory:   "3Gi"
#     pods:            "15"
#     services:        "10"
#     persistentvolumeclaims: "2"
